{
  "name": "leanderso",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "node src/build.mjs",
    "dev": "npm run build && python3 -m http.server 4173 --bind 127.0.0.1 --directory dist"
  },
  "engines": { "node": ">=22" }
}
